
import 'package:flutter/material.dart';

void main() {
  runApp(const pgm());
}
class pgm extends StatelessWidget {
  const pgm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Material App
    return MaterialApp(

      // Scaffold Widget
        home: Scaffold(

          body: const Center(child: Text('Hello Worlds')),
        ));
  }
}
